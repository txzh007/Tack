const state = {
  isAdd: false, // 新增操作标识符
  isEdit: false, // 编辑
  isView: false, // 查看
  selectItems: [], // 模块选择
}

const mutations = {}

const actions = {}

export default {
  namespaced: true,
  state,
  mutations,
  actions,
}
